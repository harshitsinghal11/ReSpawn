<Window x:Class="ReSpawn.Views.AddGameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Game" Height="360" Width="480"
        WindowStartupLocation="CenterOwner"
		KeyDown="Window_KeyDown"
        ResizeMode="NoResize"
        Background="#1a1a2e">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Add Game" FontSize="20" FontWeight="Bold"
                   Foreground="White" Margin="0,0,0,20"/>

        <!-- Browse -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding ExePath}" IsReadOnly="True"
                     Background="#16213e" Foreground="#aaaaaa" BorderBrush="#444"
                     Padding="8,6" VerticalContentAlignment="Center"/>
            <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseCommand}"
                    Margin="8,0,0,0" Padding="12,6"
                    Background="#e94560" Foreground="White" BorderThickness="0"/>
        </Grid>

        <!-- Game Name -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Game Name" Foreground="#aaaaaa" FontSize="12" Margin="0,0,0,4"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                     Background="#16213e" Foreground="White" BorderBrush="#444"
                     Padding="8,6"/>
        </StackPanel>

        <!-- Process Name -->
        <StackPanel Grid.Row="3" Margin="0,0,0,12">
            <TextBlock Text="Process Name (filename without .exe)" 
                       Foreground="#aaaaaa" FontSize="12" Margin="0,0,0,4"/>
            <TextBox Text="{Binding ProcessName, UpdateSourceTrigger=PropertyChanged}"
                     Background="#16213e" Foreground="White" BorderBrush="#444"
                     Padding="8,6"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" 
            HorizontalAlignment="Right" Margin="0,16,0,0">
            
			<Button Content="Cancel" Command="{Binding CancelCommand}"
            Padding="16,8" Margin="0,0,8,0"
            Background="#555555" Foreground="White" 
            BorderThickness="0" FontSize="13"
            MinWidth="80" MinHeight="34"/>

			<Button Content="Add Game"
        Command="{Binding ConfirmCommand}"
        MinWidth="90" MinHeight="34"
        FontSize="13"
        BorderThickness="0">
				<Button.Template>
					<ControlTemplate TargetType="Button">
						<Border x:Name="border"
								Background="#555555"
								CornerRadius="0"
								Padding="16,8">
							<TextBlock x:Name="txt"
									   Text="Add Game"
									   Foreground="#999999"
									   HorizontalAlignment="Center"
									   VerticalAlignment="Center"
									   FontSize="13"/>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsEnabled" Value="True">
								<Setter TargetName="border"
										Property="Background"
										Value="#e94560"/>
								<Setter TargetName="txt"
										Property="Foreground"
										Value="White"/>
								<Setter TargetName="border"
										Property="Cursor"
										Value="Hand"/>
							</Trigger>
							<MultiTrigger>
								<MultiTrigger.Conditions>
									<Condition Property="IsEnabled" Value="True"/>
									<Condition Property="IsMouseOver" Value="True"/>
								</MultiTrigger.Conditions>
								<Setter TargetName="border"
										Property="Background"
										Value="#c73652"/>
							</MultiTrigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Button.Template>
			</Button>
			
        </StackPanel>
    </Grid>
</Window>