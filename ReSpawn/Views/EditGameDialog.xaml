<Window x:Class="ReSpawn.Views.EditGameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Game" Height="320" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1a1a2e">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Edit Game" FontSize="20" FontWeight="Bold"
                   Foreground="White" Margin="0,0,0,20"/>

        <!-- Browse -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" Text="{Binding ExePath}" IsReadOnly="True"
                     Background="#16213e" Foreground="#aaaaaa" BorderBrush="#444"
                     Padding="8,6" VerticalContentAlignment="Center"/>
            <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseCommand}"
                    Margin="8,0,0,0" Padding="12,6"
                    Background="#e94560" Foreground="White" BorderThickness="0"/>
        </Grid>

        <!-- Game Name Only -->
        <StackPanel Grid.Row="2" Margin="0,0,0,16">
            <TextBlock Text="Game Name" Foreground="#aaaaaa" 
                       FontSize="12" Margin="0,0,0,6"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                     Background="#16213e" Foreground="White" BorderBrush="#444"
                     Padding="8,6"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Cancel" Command="{Binding CancelCommand}"
                    Padding="16,8" Margin="0,0,8,0"
                    Background="#555555" Foreground="White"
                    BorderThickness="0" FontSize="13"
                    MinWidth="80" MinHeight="34"/>
            <Button Content="Save Changes" Command="{Binding ConfirmCommand}"
                    Padding="16,8"
                    Background="#e94560" Foreground="White"
                    BorderThickness="0" FontSize="13"
                    MinWidth="100" MinHeight="34"/>
        </StackPanel>
    </Grid>
</Window>